export interface DataObject {
  c: Array<number>;
  h: Array<number>;
  l: Array<number>;
  o: Array<number>;
  s: string;
  t: Array<number>;
  v: Array<number>;
}

export interface QuoteObject {
  c: number;
  h: number;
  l: number;
  o: number;
  pc: number;
  t: number;
}